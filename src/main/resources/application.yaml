ktor:
    application:
        modules:
            - com.eynnzerr.ApplicationKt.module
    deployment:
        port: 18080

storage:
    driverClassName: "org.postgresql.Driver"
    jdbcURL: "jdbc:postgresql://localhost:5432/bandoriscription" # Modify if needed
    user: "postgres" # Environment variable override: DB_USER
    password: "309309" # Environment variable override: DB_PASSWORD
    maximumPoolSize: 10

bandoristation:
    externalApiUrl: "https://server.bandoristation.com"

cleanup:
  retention_days: 1
  interval_hours: 24

jwt:
    secret: "123456"
    # Environment variable override: JWT_SECRET
    issuer: "bandoriscription"
    audience: "bandoristation_mobile"
    realm: "Access to bandoriscription"
